# Dependencias externas de Python
tabulate>=0.9.0

# Módulos estándar de Python utilizados:
#   - pwd, grp (lectura de usuarios y grupos del sistema)
#   - subprocess (ejecución de lastlog)
#   - datetime (timestamps)
#   - json (exportación de reportes)
